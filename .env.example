# Copy this file to .env and set your Anthropic API key.
# The API key is consumed by LiteLLM to call api.anthropic.com.
# It is never forwarded to Claude Code clients directly.
ANTHROPIC_API_KEY=sk-ant-...
